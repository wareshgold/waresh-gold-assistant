# Waresh Gold Website v2 — Snapshot 2026-09-02

## Git
- Repository: `wareshgold/waresh-gold-assistant`
- Active development branch: `feature/website-v2`
- Snapshot created: 2026-09-02

## Production
- Domain: `https://wareshgold.ir/`
- Status: **ONLINE / VERIFIED**
- Latest known-good manual deployment Version ID: `4b00b72e-34be-49dc-bfd9-3e906040494c`
- Worker: `waresh-gold-website`
- Deployment traffic: 100%
- Deployment error rate: 0%

## Cloudflare routing
- `wareshgold.ir` is configured as a Worker **Custom Domain**.
- Old route `wareshgold.ir/*` was removed.
- Current configuration uses only the Custom Domain for the website hostname.

## HTTPS / Edge verification
`https://wareshgold.ir/cdn-cgi/trace` was successfully verified after the fresh manual deployment.

Observed:
- `visit_scheme=https`
- `http=http/2`
- `tls=TLSv1.3`
- `colo=LHR`
- `loc=GB`
- `warp=off`
- `gateway=off`

The previous browser error `ERR_QUIC_PROTOCOL_ERROR` was resolved after the fresh manual OpenNext deployment. No DNS/SSL destructive change was required.

## Manual deployment
Command:
```text
pnpm run deploy
```

Result:
- Next.js 16.2.11 build: PASS
- TypeScript: PASS
- OpenNext Cloudflare build: PASS
- 54 assets read
- Worker upload: PASS
- Custom Domain trigger: `wareshgold.ir`
- Version ID: `4b00b72e-34be-49dc-bfd9-3e906040494c`

## Application routes verified by local build
- `/`
- `/about`
- `/tools`
- `/api/calc`
- `/manifest.webmanifest`

## Known non-blocking issue
Cloudflare Git-integrated build is currently failing during:
```text
pnpm install --frozen-lockfile
```

Reason reported by Cloudflare:
`pnpm-lock.yaml` is not up to date with `dashboard/package.json`.

This does **not** currently break production because the known-good deployment path is the manual OpenNext/Wrangler deployment.

Do not change DNS, SSL, or HTTP/3 settings based solely on the old incident unless a new test proves it necessary.

## Current website package
- Next.js: `16.2.11`
- React: `19.2.4`
- React DOM: `19.2.4`
- `@opennextjs/cloudflare`: `^1.20.5`
- Wrangler: `^4.127.1`
- TypeScript: `^5`
- Tailwind CSS: `^4`

## Current Cloudflare config
`dashboard/wrangler.jsonc` uses:
```json
"routes": [
  {
    "pattern": "wareshgold.ir",
    "custom_domain": true
  }
]
```

## Safe continuation point
Production is healthy. The next engineering task is to fix the Cloudflare Git-integrated build / workspace lockfile mismatch without disturbing the working production deployment.
